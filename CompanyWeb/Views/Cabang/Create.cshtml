@model CompanyWeb.Models.CabangViewModel

@{
    ViewData["Title"] = "Create Cabang";
}

<div class="container mx-auto p-8 bg-white shadow-lg rounded-lg">
    <h1 class="text-3xl font-bold mb-6">Create Cabang</h1>

    <form asp-action="Create" method="post" class="space-y-4">
        <div asp-validation-summary="ModelOnly" class="text-red-500 text-sm"></div>

        <div>
            <label asp-for="NamaCabang" class="block text-gray-700 text-sm font-bold mb-2"></label>
            <input asp-for="NamaCabang" class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline" />
            <span asp-validation-for="NamaCabang" class="text-red-500 text-xs italic"></span>
        </div>

        <div>
            <label asp-for="Alamat" class="block text-gray-700 text-sm font-bold mb-2"></label>
            <input asp-for="Alamat" class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline" />
            <span asp-validation-for="Alamat" class="text-red-500 text-xs italic"></span>
        </div>

        <div>
            <label asp-for="Kota" class="block text-gray-700 text-sm font-bold mb-2"></label>
            <input asp-for="Kota" class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline" />
            <span asp-validation-for="Kota" class="text-red-500 text-xs italic"></span>
        </div>

        <div>
            <label asp-for="KodePos" class="block text-gray-700 text-sm font-bold mb-2"></label>
            <input asp-for="KodePos" class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline" />
            <span asp-validation-for="KodePos" class="text-red-500 text-xs italic"></span>
        </div>

        <div class="flex items-center justify-between">
            <button type="submit" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline">Create</button>
            <a asp-action="Index" class="inline-block align-baseline font-bold text-sm text-blue-500 hover:text-blue-800">Back to List</a>
        </div>
    </form>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
